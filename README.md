# dmg-django

## Backend
MySQL Server:
The MySQL database will suffice for the purpose of the web app since it's light, more stable, and faster at processing read-only operations compared to PostgreSQL.